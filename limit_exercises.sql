SELECT DISTINCT last_name FROM employees.employees ORDER BY last_name desc limit 10;

SELECT emp_no FROM employees.salaries order by salary DESC LIMIT 5;

SELECT emp_no FROM employees.salaries order by salary DESC LIMIT 5 OFFSET 50;

